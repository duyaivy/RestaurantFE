export default {
  theme: {
    extend: {
      colors: {
        primaryDark: '#0a0908',
       border: "hsl(var(--border))",
        ring: "hsl(var(--ring))",
        background: "hsl(var(--background))",
        foreground: "hsl(var(--foreground))",
        // ... các màu khác nếu cần
      },
    },
  },
  // ...existing config
}